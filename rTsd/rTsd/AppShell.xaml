<?xml version="1.0" encoding="UTF-8"?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms" 
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:d="http://xamarin.com/schemas/2014/forms/design"
       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
       xmlns:resources="clr-namespace:rTsd.Resources.Resx"
       mc:Ignorable="d"
       Title="rTsd"
       x:Name="RootShell"
       x:Class="rTsd.AppShell">

    <!-- #region Shell header -->

    <Shell.FlyoutHeaderTemplate>
        <DataTemplate>
            <StackLayout Orientation="Vertical" BackgroundColor="White">
                <!-- Logo -->
                <Image Aspect="AspectFill"
                       Source="logo1.jpg"/>

                <StackLayout Orientation="Vertical" 
                             Padding="20" 
                             Spacing="5" >
                    <Label Style="{StaticResource PrimaryLargeLabelStyle}" 
                           Text="#rTsd"  />

                    <Label Text="{x:Static resources:AppResources.FlyoutHeaderTitle}" 
                           FontSize="Caption" />

                    <Label Style="{StaticResource PrimaryBodyLabelStyle}"
                           Text="{x:Static resources:AppResources.FlyoutHeaderDescription}" />
                </StackLayout>
            </StackLayout>
        </DataTemplate>
    </Shell.FlyoutHeaderTemplate>

    <!--#endregion-->

</Shell>
